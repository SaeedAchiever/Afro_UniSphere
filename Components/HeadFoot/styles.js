import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
 setGoalContainer:{
  paddingTop:10,
  width:'100%',
  alignSelf:'center',
  maxWidth:700
 }
});

export default styles;
